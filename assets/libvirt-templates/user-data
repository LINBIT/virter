#cloud-config
disable_root: False
ssh_authorized_keys:
  - {{ .SSHPublicKey }}
preserve_hostname: false
hostname: {{ .VMName }}
fqdn: {{ .VMName }}.test
runcmd:
  - echo {{ .IPAddress }} {{ .VMName }} {{ .VMName }}.test >> /etc/hosts
