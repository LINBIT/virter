#cloud-config
disable_root: False
ssh_authorized_keys:
{{- range .SSHPublicKeys }}
  - {{ . }}
{{- end }}
preserve_hostname: false
hostname: {{ .VMName }}
fqdn: {{ .VMName }}.test
runcmd:
  - echo {{ .IPAddress }} {{ .VMName }} {{ .VMName }}.test >> /etc/hosts
