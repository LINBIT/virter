name: Go

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go 1.26
      uses: actions/setup-go@v6
      with:
        go-version: 1.26
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v6

    - name: Build
      run: go build -v .

    - name: Test
      run: |
        go run github.com/vektra/mockery/v3@v3.7.0
        go test ./internal/virter/

    - name: Vet
      run: go vet ./...
